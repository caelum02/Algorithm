#include <bits/stdc++.h>
using namespace std;

// bipartite matching
const int MAX_N = 10000;

typedef vector<int> vi;

int T;
vector<bool> visited;
vi A, B;

bool adj[MAX_N][3]; 

int main(){
    scanf("%d", &T);
    while(T--){
        int N, W;
		scanf("%d %d", &N, &W);
	
		A = vi(N); B = vi(N);

		int idx=0;	

		for(int i=)

		memset(adj, 0, sizeof(adj));
		for(int i=0; i<N; i++)
			if(enemy[i]+enemy[i+N] <= W) adj[i][2] = true;


	}
}

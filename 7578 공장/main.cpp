include <bits/stdc++.h>
using namespace std;

const int MAX_N=500000, MAX_H=1000000;
int N, hsh[MAX_H], ord[MAX_N];
typedef vector<int> vi;

void update(vi &tree, int s, int e, int idx, int d){
	
int main(){
	scanf("%d", &N);
	int t;

	for(int i=0; i<N; i++){
		scanf("%d", &t); hsh[t]=i;
	}
	for(int i=0; i<N; i++){
		scanf("%d", &t); ord[i]=hsh[t];
	}
}

